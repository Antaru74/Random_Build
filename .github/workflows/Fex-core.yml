name: Build FEX-core DEB package (ARM64 Host - Clang)

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch or tag to build'
        required: true
        default: 'main'
        type: string

jobs:
  build-fex-deb:
    # Gunakan runner ARM64 (bukan x86_64)
    runs-on: ubuntu-24.04-arm
    # Alternatif: runs-on: ubuntu-22.04-arm

    steps:
      # Step 1: Checkout FEX repository
      - name: Checkout FEX repository
        uses: actions/checkout@v4
        with:
          repository: FEX-Emu/FEX
          ref: ${{ github.event.inputs.branch }}
          fetch-depth: 1

      # Step 2: Install build dependencies (Clang + LLD + tools)
      - name: Install build dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            git \
            cmake \
            ninja-build \
            pkgconf \
            clang \
            lld \
            ccache \
            nasm \
            python3-clang \
            libstdc++-12-dev \
            checkinstall

      # Step 3: Configure with CMake using Clang and LLD
      - name: Configure with CMake
        run: |
          cmake -B Build \
            -DCMAKE_C_COMPILER=clang \
            -DCMAKE_CXX_COMPILER=clang++ \
            -DCMAKE_LINKER=/usr/bin/ld.lld \
            -DCMAKE_INSTALL_PREFIX=/usr \
            -DCMAKE_BUILD_TYPE=Release \
            -DENABLE_LTO=True \
            -DBUILD_TESTS=False \
            -G Ninja

      # Step 4: Build FEXCore
      - name: Build FEXCore
        run: |
          cmake --build Build --config Release

      # Step 5: Create deb package using checkinstall
      - name: Create DEB package
        run: |
          cd Build
          sudo checkinstall \
            --pkgname=fex-core \
            --pkgversion="git-$(git describe --tags --always | sed 's/^v//')" \
            --pkgrelease="1" \
            --pkglicense="MIT" \
            --pkggroup="misc" \
            --maintainer="'$(git log -1 --pretty=%aN <%aE>)'" \
            --provides="fex-core" \
            --requires="libc6, libstdc++6" \
            --install=no \
            --fstrans=yes \
            --nodoc \
            ninja install

      # Step 6: Upload deb package as artifact
      - name: Upload DEB package
        uses: actions/upload-artifact@v4
        with:
          name: fex-core-deb-arm64-clang-${{ github.event.inputs.branch }}
          path: Build/*.deb
